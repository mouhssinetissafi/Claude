# Core runtime (pip install -r requirements.txt)
anthropic>=1.6.0
jsonschema>=4.20
PyYAML>=6.0
scenedetect>=0.6.4
opencv-python-headless>=4.9
numpy>=1.26
pillow>=10.0

# Optional: real captions (word timestamps) - large download, CPU works
# faster-whisper>=1.0

# Optional: YouTube upload
# google-api-python-client>=2.100
# google-auth>=2.25
# google-auth-oauthlib>=1.2

# Dev / tests
pytest>=8
ruff>=0.5
mypy>=1.10
types-PyYAML
types-jsonschema
